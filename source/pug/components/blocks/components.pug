mixin components({content} = {})
  +card({modifiers: ["filled-secondary", "border-radius-xl", "elevated"]}).components.no-scrollbar&attributes(attributes)
    +grid({modifiers: ["cols-2", "gap-3xl"]})
      each item in content
        +base-card(item)
